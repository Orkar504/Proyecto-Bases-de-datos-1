@model IEnumerable<cuentasPorCobrar.Models.Detalle_Prestamos>

@{
    ViewData["Title"] = "index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monto_capital)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monto_intereses)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cuota_de_pago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IDprestamo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto_capital)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto_intereses)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cuota_de_pago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IDprestamo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
